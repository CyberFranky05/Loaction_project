# ============================================
# Vercel Frontend Environment Variables
# Add these in Vercel Dashboard → Settings → Environment Variables
# ============================================

# Backend API URL (your EC2 public IP)
# Note: Using HTTP since we don't have SSL certificate for IP addresses
PUBLIC_API_URL=http://your-ec2-public-ip

# Keycloak Auth URL (your EC2 public IP)
PUBLIC_KEYCLOAK_URL=http://your-ec2-public-ip/auth
PUBLIC_KEYCLOAK_REALM=location-auth-realm
PUBLIC_KEYCLOAK_CLIENT_ID=location-auth-frontend

# Supabase (Public keys only - safe for frontend)
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
